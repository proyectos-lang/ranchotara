import { FinancieroManager } from "@/components/financiero/FinancieroManager";

export const metadata = {
  title: "Financiero — Rancho Alba Admin",
};

export default function FinancieroPage() {
  return <FinancieroManager />;
}
