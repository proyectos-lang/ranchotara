import { UsuariosManager } from "@/components/admin/UsuariosManager";

export const metadata = {
  title: "Usuarios — Rancho Alba Admin",
};

export default function UsuariosPage() {
  return <UsuariosManager />;
}
