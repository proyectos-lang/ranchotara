import { ArticulosManager } from "@/components/articulos/ArticulosManager";

export const metadata = {
  title: "Artículos — Rancho Tara Admin",
};

export default function ArticulosPage() {
  return <ArticulosManager />;
}
